/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "files.raycast.com",
      "via.placeholder.com",
      "avatars.githubusercontent.com",
      "raycast.com",
    ],
  },
};

module.exports = nextConfig;
